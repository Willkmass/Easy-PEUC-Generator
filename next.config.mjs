/** @type {import('next').NextConfig} */
const nextConfig = {
  // Desativa os indicadores e ferramentas de desenvolvimento/plataforma
  devIndicators: {
    appIsrStatus: false,
    buildActivity: false,
  },
};

export default nextConfig;
